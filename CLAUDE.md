# Claude Code

Read `AGENTS.md` first. It is the single operating file for this repository.

Then load the canonical method from https://github.com/phllp-tanstic/hackathon-skills:
`SKILLS_INDEX.md`, then the skill matching the current phase (currently `skills/spec-driven-build/SKILL.md`).

Work one bounded task at a time from the approved spec. Report using the completion report format in `AGENTS.md`.
